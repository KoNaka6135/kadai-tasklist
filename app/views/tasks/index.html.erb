<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  
  <li>
    <p>ID:<%= link_to task.id, task %></p>
    <p>タスク:
    <%= task.content %></p>
    <p>ステータス:
    <%= task.status %></p>
  </li>
  
  <% end %>
  
</ul>

  <%= link_to '新規タスクの追加', new_task_path %>